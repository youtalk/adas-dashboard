# Recordings

One directory per run, one JSONL file per endpoint, one message per line, the first line a `hello`. Play them with `python3 -m tools.replay stack=<dir>/stack.jsonl:8090 world=<dir>/world.jsonl:8091`.

| Directory | Source | Content |
|---|---|---|
| `ces2027-kill-route/` | generated by `tools/synth.py` | 55 s total. Cruise at 12 m/s on a curving two-lane road. Forward collision warning around 23 to 28 s. VisionPilot killed at 40 s. Safety Island stops the car by 44.5 s. |

The generated run is a stand-in until the bench recording from W5 replaces it. Recordings are CC0.
